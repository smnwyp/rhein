# 回测环境与策略评估报告计划

## 目标

让项目能够对 `data/` 文件夹内的 OHLC CSV 数据批量执行现有动量突破策略，并在每次运行结束后生成一份中文评估报告，用于比较和判断策略表现。

## 实施步骤

1. **确认数据与现有策略接口**
   - 校验 `data/` 中 CSV 的日期、开高低收字段，以及现有 yfinance 多层表头兼容逻辑。
   - 保持当前策略的入场、止损、出场与不重叠持仓规则不变；将批量运行和报告功能作为其外围能力，避免无意改变策略结果。

2. **建立可复现的 Python 环境**
   - 在项目根目录创建 `.venv` 虚拟环境。
   - 建立 `requirements.txt`，先固定本项目实际所需的核心库：`pandas`、`numpy`；若报告实现确有需要，再按最小依赖原则添加库。
   - 安装依赖，并以 `.venv/bin/python` 验证能导入所有依赖。

3. **扩展命令行批量回测入口**
   - 支持将 `data/` 目录作为输入，并自动遍历 CSV；也继续兼容单个 CSV 输入。
   - 对每个数据文件独立运行回测，保留逐笔交易明细（入场、出场、持有天数、收益率、盈亏金额、退出原因）。
   - 增加清晰的参数回显，确保报告记录本次实际使用的资金、复利模式、手续费、止损与均线等设置。

4. **计算策略 KPI**
   - 交易次数：完成的开平仓交易数量。
   - 胜率：盈利交易数 ÷ 总交易数 × 100%。收益率大于 0 记为盈利；等于 0 记为非盈利。
   - 最终权益：复利模式下为 `初始资金 × ∏(1 + 每笔净收益率)`；固定仓位模式下为 `初始资金 + 各笔盈亏金额之和`。
   - 总盈利/亏损：`最终权益 − 初始资金`。
   - 平均盈利、平均亏损：分别对盈利交易与亏损（含持平）交易的净收益率求平均。
   - 盈亏比：`平均每笔盈利 ÷ |平均每笔亏损|`；没有亏损时显示为不适用/无穷大，避免除零误导。
   - Profit Factor（盈利因子）：`所有盈利金额之和 ÷ |所有亏损金额之和|`，用于衡量总盈利相对总亏损；没有亏损时同样标记为不适用/无穷大。
   - 最大回撤：权益曲线从历史最高点到后续最低点的最大百分比跌幅。
   - 补充指标：平均单笔收益、平均持仓天数、最大单笔盈利/亏损、退出原因分布；在数据跨度足够时，可再加入年化收益与夏普比率，并明确其假设。

5. **输出中文报告与机器可读结果**
   - 控制台输出简洁中文摘要，便于快速看胜率、最终权益、总盈亏、盈亏比、盈利因子和最大回撤。
   - 每次运行在 `reports/` 写入带时间戳的 Markdown 报告，包含：运行参数、逐标的 KPI 汇总、合并汇总、交易明细与每个指标的中文解释/公式。
   - 同时保存 CSV 或 JSON 的结构化结果，方便后续作图、比较参数或导入其他工具。

6. **验证与交付**
   - 以 `data/tesla.csv`、`data/nvda.csv` 运行完整流程，检查报告生成、中文字段、数值一致性和边界情况（无交易、无亏损、未平仓）。
   - 记录一条可复制的运行命令，并说明如何更改策略参数和查看最新报告。

## 报告口径

- 每笔收益率使用买卖价格产生的收益，扣除设置的双边手续费后计算。
- 当启用复利时，下一笔交易以当前权益作为本金；固定仓位时，每笔均以初始资金作为本金。因此两种模式的总盈亏可能不同，报告会明确标注模式。
- 各 CSV 先独立统计，再给出合并结果；合并结果只在相同资金口径下解释，避免把不同标的各自独立复利后的资金直接当成一个可同时交易的组合。

## 验收标准

- 新环境可用，一条命令即可安装并运行。
- 对 `data/` 可批量回测，单文件旧用法仍可用。
- 每次运行都会在 `reports/` 产生中文 Markdown 报告和结构化数据文件。
- 报告清楚给出胜率、最终权益、总盈亏、盈亏比、盈利因子、最大回撤等 KPI，并说明其计算方法和统计口径。

## 实施状态（2026-07-19）

- 已完成：`.venv`、`requirements.txt`、目录批量回测、中文 Markdown/CSV/JSON 报告、KPI 与公式说明。
- 已验证：以 `data/nvda.csv` 和 `data/tesla.csv` 全量执行；同时验证了无交易时报告正常生成。

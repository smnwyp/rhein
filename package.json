{
  "name": "rhein-regression-tests",
  "private": true,
  "scripts": {
    "test:python": "PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q",
    "test:e2e": "start-server-and-test '.venv/bin/streamlit run app.py --server.headless true --server.port 8501' http://127.0.0.1:8501 'cypress run'",
    "test:e2e:open": "cypress open"
  },
  "devDependencies": {
    "cypress": "^15.8.2",
    "start-server-and-test": "^2.1.2"
  }
}
